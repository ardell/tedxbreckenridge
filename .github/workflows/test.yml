name: Test

on:
  push:
    branches: [main]  # Only happens via PR merges (direct pushes blocked by branch protection)
  pull_request:
    branches: [main]

concurrency:
  group: test-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  test:
    name: Test Site Quality
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up mise
        uses: jdx/mise-action@v2
        with:
          experimental: true

      - name: Verify Ruby version
        run: ruby --version

      - name: Cache gems
        uses: actions/cache@v4
        with:
          path: vendor/bundle
          key: ${{ runner.os }}-gems-${{ hashFiles('Gemfile.lock') }}
          restore-keys: ${{ runner.os }}-gems-

      - name: Install dependencies
        run: |
          bundle config path vendor/bundle
          bundle install --jobs 4 --retry 3

      - name: Build Jekyll site for testing
        run: bundle exec jekyll build --config _config.yml,_config.test.yml

      - name: Validate image sizes
        run: ./build/website/validate-images.sh

      - name: Test HTML with HTMLProofer (internal links)
        run: |
          bundle exec htmlproofer ./_site \
            --disable-external \
            --allow-hash-href \
            --ignore-urls "/^http:\/\/127.0.0.1/,/^http:\/\/0.0.0.0/,/^http:\/\/localhost/" \
            --ignore-status-codes "0,999"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install pnpm
        uses: pnpm/action-setup@v4

      - name: Install JavaScript dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Puppeteer browsers
        run: pnpm exec puppeteer browsers install chrome

      - name: Lint CSS with Stylelint
        run: pnpm test:css

      - name: Test accessibility with pa11y-ci
        run: |
          pnpm serve &
          SERVER_PID=$!
          sleep 5
          pnpm test:a11y
          kill $SERVER_PID

      - name: Validate affiliate tracking links
        run: ./build/website/validate-affiliate-links.sh

      - name: Validate QR code documentation
        run: ./build/website/validate-qr-codes.sh

      - name: Validate Event structured data
        run: ./build/website/validate-structured-data.sh
